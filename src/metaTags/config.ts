const config = {
  siteName: "list.yieldbay.io",
  defaultTitle: "YieldBay",
  defaultDescription:
    "Discover and earn yield from polkadot and kusama paraverse",
  defaultDomain: "https://list.yieldbay.io",
  defaultImage: `https://list.yieldbay.io/twitter-cover.png`, // OG like twitter card generally needs full path of image, not relative
  defaultUsername: "@yield_bay",
};

export default config;
