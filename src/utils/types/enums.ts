export enum ModalType {
  DEFAULT = "default",
  BLUE = "blue",
  RED = "red",
}

export enum Method {
  PERCENTAGE = 0,
  LP = 1,
}

export enum Network {
  MOONBEAM = "moonbeam",
  MOONRIVER = "moonriver",
  ASTAR = "astar",
  HARDHAT = "hardhat",
}

export enum ChainId {
  MOONBEAM = 1284,
  MOONRIVER = 1285,
  ASTAR = 592,
  HARDHAT = 31337,
}
